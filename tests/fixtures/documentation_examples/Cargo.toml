[package]
name = "qubit-json-documentation-examples"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
qubit-json = { path = "../../.." }
qubit-budget = { version = "0.5", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[workspace]
